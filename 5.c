#include <stdio.h>
// //5
// int main() {
//     int num, count;
//     float sum, average_mean;
//     sum = 0; num = 1; count = 0;
//     while (num != 0) {
//         scanf("\n%d", &num);
//         sum = sum + num;
//         if (num != 0) {
//             count++;
//         }
//     }
//     average_mean = sum / count;
//     printf("\n%f", average_mean);
// }
// просто вписываем числа, пока они не равны нулю, затем в цикле находим сумму чисел, и в if каждый раз встречая число
//пока оно не равно нулю, считаем количество(count). После находим среднее арифметическое(average_mean) и выводим.